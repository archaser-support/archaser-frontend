/**
 * CSS font-family stack for Space Grotesk numerals.
 * Matches the CSS variable set on the dashboard shell via `next/font/google`.
 * Safe to import from client components (no next/font).
 */
export const SPACE_GROTESK_FONT_FAMILY =
    "var(--font-space-grotesk), 'Space Grotesk', sans-serif";
